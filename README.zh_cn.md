# Better Removal

[English](README.md) | 中文

Fabric 1.20.1：按住 **左 Alt**（可改键）**右键**容器，直接把物品取进背包 —— **不用打开 GUI**。

## 模式

按住 **左 Alt**（可改键）右键：

- **空手** = **取出**（或从背包**补货**）；
- **手持物品** = 把该物品**放入**支持的容器。

用**模式键**（按住后滚轮在 7 个预设间切换，松开生效；**默认未绑定**）或 `/br` 指令选择预设：

| 预设 | 指令 | 效果 |
|---|---|---|
| 取出 · 输出槽（默认） | `/br extract output` | 只取出输出槽 |
| 取出 · 输入槽 | `/br extract input` | 只取出输入槽 |
| 取出 · 燃料槽 | `/br extract fuel` | 只取出燃料槽 |
| 取出 · 全部 | `/br extract all` | 取出所有槽位 |
| 放入 · 输入槽 | `/br deposit input` | 放入输入槽 |
| 放入 · 燃料槽 | `/br deposit fuel` | 放入燃料槽 |
| 补货 | `/br restock` | 用背包里已有的同类物品补充输入/燃料槽（空槽不补） |

- `/br now` —— 显示当前预设。
- `/br`（不带参数）—— 等同于 `/br now`。
- 放入时会遵守容器自己的槽位过滤（例如熔炉的燃料槽只收燃料）。

## 支持的容器

### 取出与放入

| 容器 | 取出 | 放入（输入 / 燃料） |
|---|---|---|
| 熔炉 / 高炉 / 烟熏炉 | 是 | 输入槽 / 燃料槽 |
| 酿造台 | 是 | 材料槽 / 烈焰粉 |
| 漏斗 / 发射器 / 投掷器 | 是 | 每个槽位 |
| 农夫乐事 篮子 | 是 | 每个槽位 |
| 农夫乐事 厨锅 | 是 | 输入槽 |
| Cooking for Blockheads 烤炉 | 是 | 输入 / 燃料 |
| Ad Astra 压缩机 | 是 | 输入槽 |
| Ad Astra 电子高炉 | 是 | 各输入槽 |
| Ad Astra 燃油精炼机 | 是 | 流体 + 物品输入槽 |
| Ad Astra 氧气装载机 | 是 | 流体 + 物品输入槽 |
| Ad Astra 低温冷冻机 | 是 | 各输入槽 |
| Crabber's Delight 蟹笼 | 是 | 诱饵槽 |
| 天境 冷冻机 / 祭坛 | 是 | 输入 / 燃料 |
| Vinery 发酵桶 | 是 | 各输入槽 / 酒瓶槽 |
| Vinery 苹果压榨机 | 是 | 各输入槽 / 酒瓶槽 |
| 沉浸农艺 烹饪锅 | 是 | 输入 / 碗 |
| 沉浸农艺 烤炉 | 是 | 输入 / 碗 |
| 沉浸农艺 炉灶 | 是 | 输入 / 燃料 |
| 化石与考古 分析仪 | 是 | 各输入槽 |
| 化石与考古 筛子 | 是 | 输入槽 |
| 化石与考古 培养槽 | 是 | 输入 / 燃料 |
| 化石与考古 工作台 | 是 | 输入 / 燃料 |

---

## 配置

通过 **Configured** 模组修改。

---

## 兼容性

### FTB Ultimine

安装 **FTB Ultimine** 后，同时按住**两个键** —— Better Removal 的修饰键（左 Alt，可改键）
**以及** Ultimine 键（默认 `~`）—— 再右键一个受支持的容器，就会对**当前 Ultimine 形状里的
每一个受支持容器**都操作一次。
> 注意：FTB Ultimine 必须在客户端和服务端都安装，因为按键状态和连锁形状都是服务端管理的。

### Jade

安装 **Jade** 后，按住修饰键看向受支持的容器，提示框里会多出一行高亮预览 —— 即将取出的物品
（空手）或即将放入的物品（手持物品 + 放入模式）。当会触发 FTB Ultimine 连锁取出时，预览显示的是
所有连锁容器物品的总和。可以在 Configured 配置里用 **Jade 预览** 开关关掉。

> 注意：想让取出预览显示真实容器内容，**Jade 必须在客户端和服务端都安装**。

---

## 实验性：通用容器支持

内置的逐模组支持之外，还有一条**实验性**路线，让*任意*容器都能用，而不必为每个模组写代码。
它**默认关闭**，由三部分组成：**声明式槽位规则**、**自动探测**，
以及帮你生成规则的**主动探测模式**。

| 配置项（Configured） | 默认 | 含义 |
| --- | --- | --- |
| `自动探测容器` | 关 | 整个实验性功能的总开关 |
| `自动探测白名单` | 空 | 只有列在这里的方块才会被自动探测 |
| `主动探测模式` | 关 | 模式滚轮里多出「主动探测」|
| `容器槽位规则` | 空 | 手写的槽位表，一行一条 |

### 1. 声明式槽位规则

规则直接说明容器的每个槽位是什么，不需要任何猜测。规则有三个来源，优先级从高到低：

1. Configured 里的 `容器槽位规则` —— 一行一条，游戏内就能改；
2. `config/better-removal/containers/*.json` ；
3. 模组 jar 里的 `better-removal/containers/*.json` 。

三种来源都用同一套匹配语法，且忽略大小写：`模组ID:方块ID` 匹配单个方块。

Configured 的写法：`<匹配串> <角色>=<槽位> ...`，槽位用逗号分隔，`*` 表示所有槽位：

```
ironfurnaces input=0 fuel=1 output=2
somemod:crusher,somemod:grinder input=* output=9,10
```

JSON 文件写法（一条规则的 `match` 用逗号分隔即可同时匹配多个方块）：

```json
{
  "containers": [
    { "match": "somemod:crusher", "input": [0, 1], "fuel": [2], "output": [3, 4], "ignore": [5] },
    { "match": "somemod:trash_can", "ignore": "*" }
  ]
}
```

角色有 `input`、`fuel`、`output` 和 `ignore`（等同 `none`）。
后面的赋值覆盖前面的，所以 `"output": "*", "input": [1]` 表示"除了 1 号槽，其它都是输出槽"。
超出范围的槽位编号会被忽略；写错的规则会被跳过。

规则**不需要**白名单，但仍受总开关约束。模组内置了一条
**Iron Furnaces** 的规则（`better-removal/containers/iron_furnaces.json`：输入 `0`、燃料 `1`、
输出 `2`，其余忽略），所以只要打开实验性开关，这个模组就整组可用。`/br reload` 会重新读取所有规则文件与 Configured 列表。

### 2. 自动探测（兜底猜测）

只有列在**白名单**里的方块会被自动探测，写法与规则匹配串完全一致，。

三个探测后端依次执行：

1. **菜单只读。** 构造容器自己的菜单，读取模组作者写下的声明（能否放入 / 能否取出 / 槽位是否显示 /
   堆叠上限）。
2. **主动搬运（可选，需要 `主动探测模式`）。** shift 点击槽位是由菜单自己的 `quickMove` 实现的，这一步把探测物放进菜单的
   *一次性*玩家背包、跑一次 `quickMove`，然后看它落进了哪个容器槽。这样能直接读出模组自己的逻辑，
   对那些 `isValid` 一律回答"是"的机器同样有效。
3. **原版容器接口。** 兜底方案，只用几件常见物品问 `Inventory.isValid`，并检查 `SidedInventory.canExtract`。

结果（以及用的是哪个后端，例如 `menu`、`menu+transfer`、`inventory`）会打印到日志：
`[auto-detect] <方块> -> 0=INPUT 1=FUEL 2=OUTPUT ...`，可以先确认它判断得对不对再决定要不要用。
如果某台机器被探测时出问题，就把 `主动探测模式` 关掉。

### 3. 主动探测模式（快速手写一条规则）

打开 `主动探测模式` 后，模式滚轮会多出一项：**主动探测**。在这个模式下按住修饰键右键任意容器，
服务端会把上面三个探测后端各跑一次，然后把结果打印到聊天框，每个后端一行规则 JSON，复制即用：

```
主动探测：somemod:crusher
① 菜单只读: {"match":"somemod:crusher","input":[0,1],"fuel":[2],"output":[3]}
② 主动搬运: {"match":"somemod:crusher","input":[0],"fuel":[2]}
③ 库存接口: {"match":"somemod:crusher","output":[3,4]}
```

挑看起来正确的那一行，粘进 `config/better-removal/containers/` 下的 json（或 Configured 的
`容器槽位规则` 列表），`/br reload` 后生效。如果某个后端什么都判断不出来，那一行会显示 `无法探测`。
探测不写任何文件，容器也会保持原样。

### 4. 范围与限制

- 只要槽位能编号，奇特的布局也没问题：多个输入、多个输出只是在同一条规则里多写几个数字
  （Ad Astra 的燃油精炼机、各种锅和烤炉），没有燃料槽的机器不写 `fuel` 就行。
- 规则够不到的，是游戏无法逐槽寻址的容器：方块实体根本没实现 `Inventory`，或者 GUI 的槽位指向
  内部的私有容器而不是方块实体本身（农夫乐事的厨锅、Cooking for Blockheads 的烤炉）。
  这些继续靠内置的专用支持。

---

## 许可

MIT License —— Copyright (c) 2026 CloseDW

见 [LICENSE](LICENSE)。
